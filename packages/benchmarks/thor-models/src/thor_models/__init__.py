"""thor-models — model registry, model zoo and optimization tooling.

The built-in zoo mirrors the benchmark workloads and adds deployment
metadata (licenses, task types). Optimization currently produces
profiles/plans; engine building (TensorRT, quantization) lands with the
optimization toolchain in a later sprint.
"""

__version__ = "0.1.0"
