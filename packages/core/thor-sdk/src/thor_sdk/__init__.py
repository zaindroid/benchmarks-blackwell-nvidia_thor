"""thor-sdk — device communication, power monitoring and telemetry.

Wraps thor-core hardware monitoring with a device-oriented API and adds
host-level telemetry collection (CPU, disk, network) via psutil.
"""

__version__ = "0.1.0"
